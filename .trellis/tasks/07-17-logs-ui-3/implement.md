# Implement: 请求日志 UI 3.0

1. Update `index.html` logs panel structure (hero, density, col classes).
2. Implement `renderLogsHero` + compact/selected rows in `render-list.js`.
3. Progressive trace hero in `render-trace.js`.
4. Bind density + hero actions in `bind-logs.js`.
5. Add `css/logs-ui3.css` and register.
6. Run `npm run verify` then `npm run test:e2e`; fix only contract-breaking failures.
