# Audit empty recovery secondary CTA

## Goal

True-empty admin audit list offers clearer recovery copy plus a secondary path to generate evidence (open keys pool), not refresh-only.

## Evidence

- Filtered empty: clear + refresh
- True empty: only refresh-audit
- ui-ux-pro-max: empty states need message + multiple recovery paths when useful

## Requirements

1. Improve true-empty copy: complete a key/export action to create evidence
2. Secondary ghost: 打开密钥池 → switchTab keys
3. Keep primary 刷新列表
4. Sync index.html placeholder; unit pin; screenshots; verify 110 + e2e 7

## Acceptance

- [ ] true empty has refresh + open-keys
- [ ] handler switches to keys tab
- [ ] verify 110 + e2e 7
