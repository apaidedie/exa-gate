# Proxy flow node live aria labels

## Goal

Proxy-flow map nodes announce current stage value in aria-label after render (not only static HTML).

## Acceptance

- [ ] setProxyFlowNode updates aria-label with stage + value + next action
- [ ] unit pins
- [ ] verify 110 + e2e 7
