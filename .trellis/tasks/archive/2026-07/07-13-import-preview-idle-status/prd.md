# Import preview idle status phrasing

## Goal

Import modal idle status uses short 待* phrasing consistent with console idle status.

## Targets

- import preview idle state label + aria-label
- import file name idle default

## Acceptance

- [ ] 等待输入 → 待输入 (and related idle file text if needed)
- [ ] unit + e2e pins
- [ ] verify 110 + e2e 7
